---
import '../styles/index.css'
import Layout from '../components/Layout.astro'
import Hero from '../components/Hero'
import { product } from '@viberunner/common'
---

<Layout
	title={`${product.productName} - Build personal desktop apps in seconds`}
	description={`${product.productName} is a new and unique way to experience your desktop. Dream up your own collection of personal desktop apps in seconds!`}
	currentPage="home"
>
	<main class="container">
		<Hero client:load />
	</main>
</Layout>
